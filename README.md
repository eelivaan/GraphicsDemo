Some lightweight 3D-graphics demo applications made using Python and ModernGL.

Originally made back in 2024, now published here for others to see. 

Dependencies:
~~~bat
pip install moderngl
pip install moderngl-window
~~~
